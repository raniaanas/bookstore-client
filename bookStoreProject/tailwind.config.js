/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{html,ts}", // This tells Tailwind to look at all HTML and TypeScript files in src
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  